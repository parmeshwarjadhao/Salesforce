trigger : none

pool:
  vmImage : ubuntu-latest

steps:

- script: |
      sudo apt update
      apt show openssl
      sudo apt install openssl -y
      sudo apt-get install libssl-dev
      ls
      openssl version

#- task: UseNode@1
#  inputs:
#    checkLatest: true

- script:  |
   echo 'openssl-path'
   cd /home/vsts/work/openssl
   ls
   cd /home/vsts/work
   ls

   cd /usr/bin/openssl
   ls
   echo '1current directory'
   pwd
   cd /usr/bin
   ls
   echo '2current directory'
   pwd
   cd /usr/local
   echo '3current directory'
   pwd

   cd /home/
   echo '4current directory'
   pwd
   ls

   echo 'OpenSSL path'
   cd /home/vsts/work/_temp
   echo '5current directory'
   pwd
   ls
#- script: |
  # npm install -- global @salesforce/cli
  # echo 'sf cli installed'
